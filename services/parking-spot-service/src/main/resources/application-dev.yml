spring:
  datasource:
    url: ${LOCAL_DB_URL:jdbc:mysql://localhost:3306/parkease_parking_spot_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
    username: ${LOCAL_DB_USERNAME:root}
    password: ${LOCAL_DB_PASSWORD:pallavi}
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: ${LOCAL_JPA_SHOW_SQL:false}
  flyway:
    clean-disabled: false
    out-of-order: false
